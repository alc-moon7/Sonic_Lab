abstract final class DurationConstants {
  static const Duration splashDuration = Duration(milliseconds: 2500);
  static const Duration splashLogoEntrance = Duration(milliseconds: 800);
  static const Duration splashBarPulse = Duration(milliseconds: 600);
  static const Duration glowPulse = Duration(milliseconds: 800);
  static const Duration scanSweep = Duration(milliseconds: 1500);
  static const Duration dustShake = Duration(milliseconds: 200);
  static const Duration snackbar = Duration(milliseconds: 2200);
  static const int manualPreviewMs = 10000;
  static const int speakerTestMs = 5000;
  static const List<int> cleaningDurationsSeconds = <int>[15, 30, 60];
}
